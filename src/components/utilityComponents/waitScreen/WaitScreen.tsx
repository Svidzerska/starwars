import React from "react";

import "./waitScreen.scss";

const WaitScreen: React.FC = (): JSX.Element => {
  return (
    <main className="waitScreen">
      <p>Please wait...</p>
    </main>
  );
};

export default WaitScreen;
